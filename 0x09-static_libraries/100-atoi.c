#include "main.h"

/**
 * _atoi - a function that convert a string to an integer.
 * @s: string
 *
 * Return: int 0
 */
int _atoi(char *s)
{
	int i = 0;
	if (*(s + i ) == '\0')
		i++;
	return (0);
}

